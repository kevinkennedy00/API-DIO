spring:
  datasource:
    url: jdbc:mysql://roundhouse.proxy.rly.network:12350/railway
    username: root
    password: maioral747@A
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
